# Engine core (schema, forecast, covenant, vat, config, stub) is pure stdlib.
numpy            # engine/learn.py - the two calibrated coefficients
duckdb           # engine/load.py - read reconciled parquet/duckdb tables (CSV needs nothing)
streamlit>=1.36  # app.py - the role-based dashboard

# Only for inspecting raw .xlsx exports (ingestion input, not the engine):
# openpyxl
# pandas
