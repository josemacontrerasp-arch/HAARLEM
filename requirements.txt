# Engine core (schema, forecast, covenant, vat, config, stub) is pure stdlib.
numpy            # engine/learn.py — interpretable ML coefficients

# Loading Lane A's real output:
duckdb           # read parquet / duckdb tables in engine/load.py (CSV needs nothing)

# Only for inspecting raw .xlsx exports (Lane A's input, not the engine):
# openpyxl
# pandas
